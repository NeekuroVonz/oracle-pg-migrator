Ora2Pg-compatible deterministic conversion. Converts extracted Oracle DDL only.

Never opens an Oracle session. If `ORA2PG_BIN` is set, the worker may call `ora2pg -i` file mode and fall back to the TypeScript engine. PostgreSQL compile (Phase 5) is the judge of generated SQL.
